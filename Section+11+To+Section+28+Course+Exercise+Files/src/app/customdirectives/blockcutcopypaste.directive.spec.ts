import { BlockcutcopypasteDirective } from './blockcutcopypaste.directive';

describe('BlockcutcopypasteDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockcutcopypasteDirective();
    expect(directive).toBeTruthy();
  });
});
