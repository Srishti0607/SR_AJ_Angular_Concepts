import { DoValidationsDirective } from './do-validations.directive';

describe('DoValidationsDirective', () => {
  it('should create an instance', () => {
    const directive = new DoValidationsDirective();
    expect(directive).toBeTruthy();
  });
});
